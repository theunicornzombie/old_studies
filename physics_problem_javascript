<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  </head>
    <body>
      <script>
      // Algoritmo Fctp
      // Declaração de variavel
      // Resposta
      var Resposta

      //Enquanto (Resposta != 4m)
      while ((Resposta != "4m"))
      {

      // Escreva "A posição de uma partícula é dada pela expressão x= 4 cos
      (3πt + π), sendo x em metros e t em segundos. Qual a amplitude do movimento?"
      Resposta = prompt ("A posição de uma partícula é dada pela expressão x= 4
      cos (3πt + π), sendo x em metros e t em segundos. Qual a amplitude do movimento?")
      
      // Se resposta = 4m
      if (Resposta == "4m")
      {

      //Escreva "Muito bem!"
      alert("Muito bem!")
      //Fim Enquanto
      }

      //Quando (Resposta != 4m)
      else ((Resposta != "4m"))
      {

      //Escreva "Tente novamente..."
      alert("Tente novamente...")
      //Fim Quando
      }

      //Fim Enquanto
      }

    </script>
  </body>
</html>
